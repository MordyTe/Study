# Vercel serverless deployment (cloud mode) — keep lean for the size limit.
# Local mode installs the fuller set via `pip install -e .` (pyproject.toml).
fastapi>=0.110.0
numpy>=1.26.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyYAML>=6.0
python-dotenv>=1.0.0
pybit>=5.7.0
httpx>=0.27.0
google-genai>=1.0.0
